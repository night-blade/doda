$(function () {

    const vm = new Vue({
        el: '#app',
        data: {
            // Store the user's message,
            // the error type (if any),
            // and a loading indicator here
        },
        methods: {
            // Handle behavior here
        }
    })

});